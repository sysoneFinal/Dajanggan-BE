<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dajanggan.domain.vacuum.repository.VacuumHistoryMapper">

    <!-- Vacuum History 목록 조회 -->
    <!-- Vacuum History 목록 조회 -->
    <select id="getVacuumHistoryList" resultType="com.dajanggan.domain.vacuum.dto.VacuumHistoryDto$Raw">
        SELECT
            database_id AS databaseId,
            last_vacuum AS lastVacuum,
            last_autovacuum AS lastAutovacuum,
            dead_tuple_total AS deadTuples,
            n_mod_since_analyze AS modSinceAnalyze,
            bloat_bytes AS bloatBytes,
            bloat_ratio AS bloatRatio,
            -- table_size_bytes는 vacuum_trend_metrics에 없음
            -- 필요하다면 vacuum_raw_metrics의 relsize_total_bytes 사용
            vacuum_count_24h AS vacuumCount24h,
            autovacuum_count_24h AS autovacuumCount24h
        FROM vacuum_trend_metrics
        WHERE collected_at BETWEEN #{start} AND #{end}
          AND database_id IS NOT NULL
        ORDER BY collected_at DESC
            LIMIT 100
    </select>

    <!-- Vacuum 실행 빈도 계산 -->
    <select id="getVacuumFrequency" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(vacuum_count_24h + autovacuum_count_24h), 0)
        FROM vacuum_trend_metrics
        WHERE database_id = #{databaseId}
          AND collected_at >= NOW() - INTERVAL '${hours} hours'
    </select>

</mapper>