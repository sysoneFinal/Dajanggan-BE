spring:
  datasource:
    url: ${GOOGLE_DB_URL}
    username: ${GOOGLE_DB_USERNAME}
    password: ${GOOGLE_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

server:
  port: 8080

logging:
  level:
    root: INFO
    com.yourpackage: DEBUG